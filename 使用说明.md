# AI 跨学科实时翻译席 · 使用说明

> 适用版本：架构设计 v1.2（流式 ASR + taiji hy3 + 纯投屏显示，暂无 TTS）
> 面向对象：现场操作员、主持人、技术负责人
>
> **部署提示**：本仓库为**单一 Go 版**（`go/` 下 `CGO_ENABLED=1 go build -o seat .`，或用 `Dockerfile` 构建镜像，详见 `README.md`）。
> 页面操作部分（控制台 / 大屏用法）本文全部适用；早期的 Python 版已随 Go 版稳定后移除。

---

## 1. 系统是什么

一个**本地运行的 Web 应用**，用于科学会议现场。它实时听取报告与问答，在主持人召唤后，由大模型生成**跨学科短翻译 / 桥接问题**，投屏到大屏供主持人现场口播、供听众理解。

核心特点：

- **实时听**：持续转写现场语音。
- **受控说**：AI 默认静默，只有操作员点按钮（或主持人召唤后）才生成并展示。
- **短输出**：每次 20–30 秒口播量，只做一件事（术语翻译 / 问题转译 / 回答转译 / 桥接提问 / 报告总结）。
- **专家确认**：所有 AI 输出都以「请报告人确认」结尾，大屏显著标注「AI 跨学科翻译尝试」。
- **可打断**：随时急停。

---

## 2. 环境准备（一次性）

### 2.1 前置依赖

- macOS 或 Linux 笔记本
- 网络可达 `asr.example.com`（ASR 与 LLM 服务）
- 音频输入设备（内置麦 / USB 麦 / 调音台 line-out）

### 2.2 安装与配置

```bash
cd conference_assistant

# 首次：复制配置并填写（默认已配好 taiji，一般除 key 外无需改）
cp .env.example .env
```

`.env` 默认已配好：

```
LLM_BASE=https://asr.example.com/tencent/llm/taiji
LLM_KEY=xxxxx
LLM_MODEL=hy3
```

### 2.3 环境自检

服务启动后访问健康接口即可（页面页脚也实时显示各状态）：

```bash
curl -s http://127.0.0.1:8081/api/health
```

---

## 3. 启动

```bash
cd conference_assistant/go
CGO_ENABLED=1 go build -o seat .
./seat
```

启动成功后（端口按 `.env` 的 `PORT`，默认 **8080**）：

- **操作员控制台**：<http://127.0.0.1:8080/console>（主屏打开）
- **会场投屏大屏**：<http://127.0.0.1:8080/screen>（扩展屏/投影全屏打开，`Cmd/Ctrl + F` 进入浏览器全屏）

> 换端口：`PORT=9000 ./seat`；仅本机访问：`HOST=127.0.0.1 ./seat`

### 远端 / 浏览器收音模式（服务器无麦克风时）

容器部署默认绑定所有网卡（`HOST=0.0.0.0`），远端浏览器可直接打开 `http://<服务器IP>:<端口>/console`。

音源由顶栏的**「浏览器收音」开关**决定（**默认勾选**），改动在下次点「开始 Session」时生效：

1. 控制台保持**「浏览器收音」勾选**，点「开始 Session」
2. 浏览器会**自动请求麦克风权限**并开始采集——音频经 WebSocket 推送到服务器识别，底部状态条显示“采集中（浏览器）”，**无需再点其他按钮**
3. 取消勾选则改用**服务器本机声卡**；服务器无音频设备时会自动降级为该模式（状态条有提示，前端同时自动开麦）
4. 采集期间顶栏出现**「停止收音」**按钮，可随时关闭浏览器麦克风（服务端流水线继续运行）

---

## 4. 会前准备

### 4.1 开始 Session

1. 在控制台顶部输入框填：
   - **科学家姓名**（必填，用于生成专属热词）
   - **机构**（可选，如「西湖大学」）
2. 点击 **「开始 Session」**。

系统会自动：
- 用 LLM 检索该科学家的研究背景；
- 生成 **10~25 个专属热词**（核心术语/方法/缩写），提升 ASR 识别准确率；
- 生成**研究背景简介**，作为大模型翻译的上下文。

> 耗时约几秒到十几秒（取决于模型），期间页面显示「正在生成…」。
> 生成失败会自动降级（用全局兜底热词），不影响开会。

### 4.2 补充会议材料（可选，强烈建议）

若要提升翻译质量，可把**报告摘要 / PPT 文本 / 术语表**放入 session 材料目录：

```
sessions/<session_id>/materials/
```

支持的格式：`.md` / `.txt` / `.json`（PDF、PPTX 需先转成文本）。

> session_id 在创建成功后显示在页面底部状态条（形如 `3ce5204f6c70`）。
> 放好后内容会自动进入 AI 的上下文（会前放的越全，翻译越准）。

### 4.3 重新生成热词

若换了报告人，或想刷新热词，调用：

```bash
curl -X POST http://127.0.0.1:8081/api/hotwords/generate \
  -H "Content-Type: application/json" \
  -d '{"name":"颜宁","institution":"深圳医学科学院","discipline":"结构生物学"}'
```

---

## 5. 页面说明

### 5.1 操作员控制台（主屏，3 栏）

| 区域 | 内容 |
|---|---|
| 左栏「实时转写」 | 现场语音实时字幕：灰色斜体=临时草稿，黑色=已确认终稿。**点击终稿可修改错字**（修正后的文本会进入 AI 上下文）。开启说话人区分后，每句上方会显示「说话人 N」，换人处用虚线分隔 |
| 中栏「任务 · 候选术语」 | 5 个任务按钮 + 目标输入框 + 自动提取的候选术语（点击术语直接触发「术语翻译」）；**底部是「发言」表单**（立场 + 语言 + 长度 + 补充要求） |
| 右栏上部「AI 输出卡」 | AI 生成的文本实时滚动显示，附字数/预估时长/校验结果，底部有展示/丢弃/重生成按钮 |
| 右栏下部「发言稿」 | 点「发言」生成的发言稿显示在这里（可编辑、重生成、投屏、复制）。它与上面的 AI 输出卡互不干扰 |
| 底部状态条 | ASR/LLM/收音/说话人状态、session 信息、调用次数 |

### 5.2 会场投屏大屏（只读，深色）

- 上部 70%：AI 大字卡（任务类型徽标 + 固定角标「AI 跨学科翻译尝试 · 待报告人确认」；
  若投的是发言稿，角标自动变为「AI 起草发言稿 · 供发言人修改使用」）
- 下部 25%：实时字幕（滚动 2–3 行）；开启说话人区分后，句子前会带「说话人 N」，换人处左侧加竖线缩进
- 顶栏：状态灯（🟢静默听会 / 🟡生成中 / 🔵展示中）+ 会议标题 + 计时

---

## 6. 会议中操作

### 6.1 五个任务

| 快捷键 | 任务 | 使用时机 |
|---|---|---|
| `1` | 术语翻译 | 报告/问答出现难懂的专业概念时 |
| `2` | 问题转译 | 听众问题过于专业时 |
| `3` | 回答转译 | 报告人回答过于专业时 |
| `4` | 桥接提问 | 问答后段，转向跨学科连接时 |
| `5` | 报告总结 | 报告结束，需要通俗总结时 |

操作流程：**按快捷键（或点按钮）→ AI 生成 → 检查输出 → 展示到大屏**。

### 6.2 生成后操作

| 操作 | 快捷键 | 说明 |
|---|---|---|
| 展示到大屏 | `Enter` | 将当前 AI 输出投到大屏，进入「展示中」状态 |
| 丢弃 | `Backspace` | 输出不理想，丢弃 |
| 重生成 | `R` | 用同一任务重新生成一次 |
| 急停 | `Esc` | 立即中止生成 / 清空大屏 AI 卡 |

> 目标术语/问题可在中栏输入框填写，或直接点击「候选术语」里的词。

### 6.3 主持人召唤的口令（建议）

对应需求文档附录，主持人可说：

- 术语翻译：*「AI 翻译席，请用 20 秒解释刚才这个术语，只做解释，不做评价。」*
- 问题转译：*「AI 翻译席，请把刚才这个问题转译成其他学科也能理解的科学问题。」*
- 桥接提问：*「AI 翻译席，请提出一个跨学科桥接问题，不评价研究质量。」*
- 暂停：*「AI，暂停。」*

主持人说完口令后，**操作员点对应按钮/快捷键**（MVP 阶段为半自动，由操作员代为触发）。

### 6.4 修正转写

若字幕出现术语/人名/缩写识别错误，操作员可在左栏**直接点击该行**修改文字，失焦即保存。修正后的文本会用于后续 AI 生成。

### 6.5 发言（按立场生成发言稿）

给与会者（或主持人）自己用的能力：**把自己要表达的立场告诉系统，由 AI 结合现场上下文写一段能直接念的发言稿。**

操作：

1. 中栏底部「立场」框填写这次发言要服务于什么观点，例如
   *「我来自电池材料方向，关心固态电解质的界面稳定性，希望找到可以合作的表征手段」*；
2. 选择**语言**（中文 / English / 中英双语）与**长度**（约 30 秒 / 1 分钟 / 2 分钟）；
3. 可选填「补充要求」，例如 *「面向政府听众」*、*「欢迎质疑」*、*「希望提到报告里的具体数据」*；
4. 点 **「发言」**（或在立场框按 `Ctrl/Cmd + Enter`）。

生成结果出现在**右下角「发言稿」区**（生成需 5~30 秒，比短翻译慢，因为稿子更长）：

| 操作 | 说明 |
|---|---|
| 直接编辑 | 点进正文即可改，失焦自动保存 |
| 重生成 | 按同样的立场与要求再生成一版 |
| 投屏 | 把这版稿子投到大屏（角标会写明「AI 起草发言稿 · 供发言人修改使用」，留屏 2 分钟） |
| 复制 | 复制全文，便于发到微信/邮件或粘进提词器 |

注意：

- 发言稿**不会自动成为会议纪要**，也没有"请报告人确认"这类强制结尾 —— 它本来就是给你念的；
- 系统仍不允许编造会议中没出现的数据/结论/人名，稿子里不确定的表述会写成"如果我没理解错"这类措辞；
- 字数与口播时长会被校验（超过所选长度会先按句截断）；校验不通过时会在右下角显示原因与模型原文，供人工取舍。

### 6.6 说话人区分（谁在说）

开启后（`.env` 里 `DIARIZE_ENABLED=true`），系统会把每句转写对应的现场语音送本机 CAM++ 模型做声纹判定：

- 同一场会议内编号稳定：**说话人 1 / 说话人 2 / …**（大屏与控制台口径一致）；
- **换人处自动断开**：控制台用虚线分隔并缩进，大屏用左侧竖线，扫一眼就知道换人了；
- 页脚「说话人:」显示状态与已识别的名单，例如 `说话人: 说话人 1、说话人 2`；
- 标记是**后补的**：先出字幕、约 0.5~2 秒后补上说话人（不影响转写速度）；
- 拿不准的句子宁可**不标**，也不会标错人；sidecar 未就绪时只是没有标记，其它功能不受影响。

现场调参（`.env`，改完重启主程序）：

- **同一个人被拆成好几个编号** → 把 `DIARIZE_THRESHOLD` 调小（如 0.4）；
- **不同的人被判成同一个编号** → 调大（如 0.6）；
- 音源本身很差（远场、混响重）时，短句容易出现跳动，可适当调大 `DIARIZE_MIN_MS` 让过短的句子不参与判定。

会话结束后，说话人表留在 `sessions/<session_id>/speakers.json`；导出（`/api/session/<id>/export`）里每段的 `speaker_hint` 就是该句的说话人编号。

---

## 7. 会议流程建议（15+15 分钟）

| 时间 | 环节 | AI 状态 | 建议 |
|---|---|---|---|
| 报告 0–15 分 | 报告人发言 | 静默听会 | 持续转写，不调用 |
| 问答 0–3 分 | 澄清问题 | 可调用 | 解释关键术语 / 通俗背景 |
| 问答 3–8 分 | 真实提问 | 谨慎调用 | 问题/回答转译 |
| 问答 8–12 分 | 专业追问 | 少用 | 留给研究员深入交流 |
| 问答 12–14 分 | 跨学科收束 | 推荐调用 | 桥接提问 |
| 问答 14–15 分 | 主持人收尾 | 可选 | 一句话提示会后方向 |

**每场建议最多调用 3–4 次**，AI 总展示时长控制在 2 分钟以内。讨论顺畅时可不调用。

---

## 8. 会后导出

```bash
curl http://127.0.0.1:8081/api/session/<session_id>/export
```

返回转写记录（含修正标记）与段落明细，可用于复盘。

> 注意：AI 输出**不会自动成为会议纪要**，只有经报告人确认的内容才应被引用。

---

## 9. 配置项速查（`.env`）

| 变量 | 默认 | 说明 |
|---|---|---|
| `ASR_PROTOCOL` | `funasr_nano` | ASR 协议（`funasr_nano` / `funasr`） |
| `FUNASR_WS_URL` | `wss://asr.example.com/s2/ws` | ASR 流式端点 |
| `ASR_LANGUAGE` | `中文` | 识别语种 |
| `LLM_BASE` | `.../tencent/llm/taiji` | LLM 端点 |
| `LLM_KEY` | `xxxxx` | LLM 密钥 |
| `LLM_MODEL` | `hy3` | 模型名 |
| `LLM_MAX_TOKENS` | `300` | 单次生成最大 token |
| `SPEECH_MAX_TOKENS` | `6000` | 「发言」单次生成预算（发言稿比短翻译长，单独给） |
| `DIARIZE_ENABLED` | `false` | 是否开启说话人区分（需 Python 环境跑 CAM++ sidecar） |
| `DIARIZE_URL` | `http://127.0.0.1:18901` | sidecar 地址 |
| `DIARIZE_THRESHOLD` | `0.5` | 同一说话人判定阈值：同人被拆开→调小，不同人并一起→调大 |
| `DIARIZE_MIN_MS` | `600` | 短于该时长的语音段不做判定 |
| `DIARIZE_AUTOSTART` | `true` | sidecar 不可达时自动拉起（优先 Go 版 `tools/diarize-go/seat-diarize`，其次 Python 版 `tools/diarize/run.sh`） |
| `DIARIZE_DEBUG` | `false` | 排查用：打印每段语音的判定与配对决策 |
| `REC_ENABLED` | `false` | 会话录音留存（排查用）：原始音频连续写 WAV 分片到 `sessions/<sid>/rec/`；含会议内容，开启前确认现场同意 |
| `REC_SEGMENT_SEC` | `300` | 录音分片时长（秒） |
| `REC_KEEP_DAYS` | `7` | 录音保留天数：超期自动清理（启动时 + 新建会话时）；`0`=不清理。占用约 115MB/小时 |
| `HOTWORDS_PATH` | `hotwords_dict.txt` | 全局热词兜底（会前自动生成 session 热词） |
| `PORT` | `8081` | 端口（启动参数，非 .env） |

---

## 10. 常见问题

| 问题 | 处理 |
|---|---|
| 启动报 `Address already in use` | 端口被占用；改用 `PORT=8081 ./seat` 或其他端口 |
| 音频采集失败 | 检查麦克风权限；或勾选「浏览器收音」改用浏览器采集（容器/服务器无麦时的标准做法） |
| 远端浏览器无法使用麦克风 | HTTP 属非安全上下文；请用 localhost/HTTPS 访问，或通过 chrome://flags（#unsafely-treat-insecure-origin-as-secure）把地址设为可信 |
| ASR 无字幕 | 确认 `FUNASR_WS_URL` 可达；看底部状态条 ASR 灯是否绿 |
| LLM 不生成 | 确认 `.env` 里 `LLM_BASE/LLM_KEY/LLM_MODEL` 已填；LLM 灯应为绿 |
| 生成太慢/超时 | 思考模型较慢，已设 30s 超时；可减少 `LLM_MAX_TOKENS` |
| 大屏断线 | 浏览器自动重连，重连后自动补齐快照，无需手动操作 |
| 术语识别差 | 检查是否填了科学家姓名（生成专属热词）；或手动修正转写 |
| 没有说话人标记 | ① 页脚看状态：`未启用`→ .env 里 `DIARIZE_ENABLED=true` 后重启；`sidecar 不可达`→ 手动 `bash tools/diarize/run.sh --port 18901` 并看 `sessions/diarize.log`；② 首次运行要装依赖+下模型（3~5 分钟），期间没有标记属正常 |
| 说话人编号乱跳 | 同一人被拆成多个编号→把 `DIARIZE_THRESHOLD` 调小；不同人并成一个→调大。远场/混响重时短句容易跳，可调大 `DIARIZE_MIN_MS` |
| 发言稿迟迟不出 | 发言稿比短翻译长，思考模型通常要 5~30 秒；若报"模型未返回正文"，调大 `SPEECH_MAX_TOKENS` |
| 发言稿太长/太短 | 换「长度」档位（约 30 秒 / 1 分钟 / 2 分钟）即可，字数窗口随之变化；超长会先按句截断 |

---

## 11. 免责与边界

- AI 输出是**跨学科翻译尝试**，不是研究结论，必须由报告人确认。
- 系统**不评价研究质量**，也不自动生成会议纪要。
- 原始音频默认**不落盘**（仅内存保留最近 120 秒，用于回溯），退出即清理。
